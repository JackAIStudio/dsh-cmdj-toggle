// dsh-cmdj-toggle — host half is intentionally empty.
// All behavior lives in the client half (client.js): a page-level hotkey that
// synthetic-clicks dsh-better-sidebar's own toggle buttons.

export const name = 'dsh-cmdj-toggle'
export const inject = []

export function apply() {}
